// Defining Array in JS

const array_name = [item1, item2, item3];

// defining Array using Square brackets
const myArray = [10, 20, 30, 40, 50];

// defining Array using Array() 
const myArray1 = new Array(1, 2, 3, 4, 5);

// In javaScript we can use the Array.from() method to create a new array from an existing array
const myString = 'hello';
const newArray = Array.from(myString);
console.log(newArray); //[ ]